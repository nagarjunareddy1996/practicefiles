1.What is salting HMAC in aws s3;
2.how to remove taints in k8s?
3. kubectl command to schedule a cronjob ?
4. If i have crated multiple security groups with terrform single state file nopw i want to splity state file into mulriple for each sg how to do ?
5. Terrform code to pull latest ami with filter?
    answer: data "aws_ami" "amazon_linux" {
      most_recent = true
    
      filter {
        name   = "name"
        values = ["amzn2-ami-hvm-*-x86_64-gp2"]
      }
    
      filter {
        name   = "virtualization-type"
        values = ["hvm"]
      }
    
      owners = ["amazon"] # Amazon-owned AMIs
    }
6. can we attach target group to auto scaling group 
ans: yes
7. what is cname , a record and aaaa record?
c name: used to map sub domains to dimain
a name: map ipv4 adress to domain
aaaa name: map ip6 adress to domain

8. can we attach multiple sg to load balancer?
yes , we can attach upto 5

9. when creating secret we need to pass base64 values in data or else we need use 
stringData instead of data field.

